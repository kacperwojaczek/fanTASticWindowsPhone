<Page
    x:Class="fanTASticWindowsPhone.EditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:fanTASticWindowsPhone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Vertical">
            <TextBlock FontSize="30">Your Post</TextBlock>
            <TextBlock></TextBlock>
            <TextBox x:Name="TitleBox" PlaceholderText="Title" Text="{Binding Title}" />
            <TextBox x:Name="ContentBox" PlaceholderText="Content" Text="{Binding Content}" />
            <Button FontSize="20" Click="Send" x:Name="SendButton"></Button>
        </StackPanel>
    </Grid>
</Page>
